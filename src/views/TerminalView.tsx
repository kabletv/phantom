import React from "react";

/**
 * TerminalView is a no-op placeholder.
 * The actual terminal is always mounted in Layout.tsx and shown via
 * display toggling to keep the PTY alive across route navigations.
 */
export function TerminalView() {
  return null;
}
